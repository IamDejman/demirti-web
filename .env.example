# Database (required for LMS and applications)
POSTGRES_URL=

# Read replica (optional, for scaling read operations; falls back to POSTGRES_URL if unset)
POSTGRES_URL_READ_REPLICA=

# Resend (email)
RESEND_API_KEY=
RESEND_FROM_EMAIL=no-reply@demirti.com

# Paystack (payments)
PAYSTACK_SECRET_KEY=

# App URL (used in links, redirects)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Cron/auth (protect init-db, init-lms)
CRON_SECRET=

# Push notifications (optional)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# AI assistant (optional)
CLAUDE_API_KEY=

# Storage (optional, for uploads)
STORAGE_BUCKET=
STORAGE_REGION=
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=
STORAGE_ENDPOINT=
STORAGE_PUBLIC_URL=

# Upstash (optional, for distributed rate limiting)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
